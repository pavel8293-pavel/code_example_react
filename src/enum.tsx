export enum tournamentStatus {
  Planning = 0,
  Open = 1,
  Close = 2,
  Completed = 3,
  Canceled = 4,
}

export enum statusUserRegistration {
  Registered = 0,
  isInWaitingList = 1,
  Canceled = 2,
}

export enum sortingType {
  desc = 'desc',
  asc = 'asc',
}